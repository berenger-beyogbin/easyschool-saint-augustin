# Dependances de developpement (tests, qualite). Installer avec :
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.6.0
pip-audit>=2.7.0
alembic>=1.13.0
